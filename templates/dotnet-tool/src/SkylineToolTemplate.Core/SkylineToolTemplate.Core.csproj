<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Cross-platform: algorithms / IO / QC. NO pipes, NO WPF - so the CLI stays portable. -->
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

</Project>
